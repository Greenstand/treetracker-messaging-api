apiVersion: v1
kind: Service
metadata:
  name: treetracker-messaging-api
spec:
  selector:
    app: treetracker-messaging-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3006
